<?xml version="1.0" encoding="utf-8"?>
<local:PageViewBase
    x:Class="Contoso.WinUI.Views.LandingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Contoso.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Spacing="8">
            <TextBlock Text="Landing" />
            <Button x:Name="SignInButton"  Command="{x:Bind ViewModel.NavigateToLoginCommand}">
                <TextBlock Text="Sign in" />
            </Button>
            <Button x:Name="SettingsButton" Command="{x:Bind ViewModel.NavigateToSettingsCommand}">
                <TextBlock Text="Settings" />
            </Button>
        </StackPanel>
    </Grid>
</local:PageViewBase>
